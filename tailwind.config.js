module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {
      colors: {
        primary: "#0073e6", // Couleur principale (bleu)
        secondary: "#00cc66", // Couleur secondaire (vert)
        accent: "#ffcc00", // Couleur d'accent (jaune)
      },
    },
  },
  plugins: [],
};
